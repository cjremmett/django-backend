# django-backend
Dummy Django backend for testing purposes

sudo docker-compose build && sudo docker-compose run web python manage.py makemigrations && sudo docker-compose run web python manage.py migrate && sudo docker-compose up -d